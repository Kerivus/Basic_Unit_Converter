<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_more"
        android:orderInCategory="100"
        android:icon="@drawable/ic_action_more"
        android:title="Favourite function"
        app:showAsAction="ifRoom|withText">

        <menu>
            <item
                android:id="@+id/action_checked"
                android:orderInCategory="100"
                android:icon="@drawable/ic_action_checked"
                android:title="Favourite All"
                app:showAsAction="ifRoom|withText"/>

            <item
                android:id="@+id/action_clear"
                android:orderInCategory="100"
                android:icon="@drawable/ic_action_clear"
                android:title="Unfavorite All"
                app:showAsAction="ifRoom|withText"/>
        </menu>
    </item>
</menu>